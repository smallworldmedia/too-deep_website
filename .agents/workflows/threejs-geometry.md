---
description: Create custom geometry, instanced meshes, or procedural shapes in Three.js
---

1. Read the geometry skill for BufferGeometry and instancing patterns:
   - `view_file .agents/skills/threejs-geometry/SKILL.md`

2. Read the materials skill for applying materials to geometry:
   - `view_file .agents/skills/threejs-materials/SKILL.md`

3. Read the textures skill if UV mapping is needed:
   - `view_file .agents/skills/threejs-textures/SKILL.md`

4. Implement the geometry with:
   - Built-in shapes or custom BufferGeometry as needed
   - InstancedMesh for many repeated objects
   - Proper vertex attributes (position, normal, uv)
   - Bounding box/sphere computation

5. Verify the geometry renders correctly in the browser.
